# Чтение и запись файлов

Что такое кодировка?

> Рассказать про `ord`

Какие есть кодировки? В чем их отличие друг от друга?


Как отличаются окончания файлов в Windows и MacOs/Unix?

Надо ли знать кодировку, чтобы открыть файл или Python сам определит?

Если ли отличие между встроенным `open()` и `Path.open()`

Какие режимы чтения файлов вы помните? Чем они отличаются?

Зачем закрыть файл после использования?

При помощи какой синтаксической конструкции можно не вызывать `file.close()` явно, а он закроется сам?

Обязательно ли создавать файл прежде чем написать в него?

Зачем писать newline="" при открытии csv файла?

В чем отличие reader и DictReader?

В чем отличие writer и DictWriter?