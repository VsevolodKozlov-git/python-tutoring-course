# Урок 33. Основы HTML: часть 1

#### Чем `<b>` отличается от `<strong>`?

- Как вы думаете, одинаково ли работают эти теги в браузере?
- Если они выглядят одинаково (жирный текст), зачем нужны два разных?

#### Чем `<em>` отличается от `<i>`?

#### Что такое атрибуты тега?


#### Можно ли на странице использовать несколько `<h1>`? Сколько их должно быть по правилам?

#### Чем `<ol>` отличается от `<ul>`?

#### Почему: `<ol>`, `<ul>`, `<li>` так называются?

#### Почему нельзя просто написать `<li>` вне списка? Зачем нужны `<ol>` и `<ul>`?

#### Зачем в таблице `<thead>` и `<tbody>`, если мы и так задаём `<td>` и `<th>`?

#### Почему бы не обойтись просто строками `<tr>` с ячейками?



#### Что нужно добавить в ячейку, чтобы она занимала несколько строк или столбцов?

```html
<table>
  <caption>Цены на флагманские модели iPhone и Xiaomi</caption>
  <thead>
    <tr>
      <th>Производитель</th>
      <th>Модель</th>
      <th>Цена</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td rowspan="3">iPhone</td>
      <td>12 Pro</td>
      <td>$999</td>
    </tr>
    <tr>
      <td>12</td>
      <td>$799</td>
    </tr>
    <tr>
      <td>12 mini</td>
      <td>$699</td>
    </tr>
  </tbody>
  <tbody>
    <tr>
      <td rowspan="2">Xiaomi</td>
      <td>Mi 10</td>
      <td>$768</td>
    </tr>
    <tr>
      <td>Black Shark 3 128 Gb</td>
      <td>$529</td>
    </tr>
  </tbody>
  <tfoot>
    <tr>
      <td>Средняя цена:</td>
      <td>$758.8</td>
    </tr>
  </tfoot>
</table>
```

#### Как отобразить символы, которые являются частью синтаксиса HTML (например, `<`), или те, которых нет на клавиатуре (например, ©)?

#### Как заставить ссылку открываться в новой вкладке? Как сделать так, чтобы она открылась в этом же окне?

#### У нас есть файлы `index.html`, `about.html`, `contact.html`. Как сделать так, чтобы с главной страницы можно было перейти на другие?

[Сайт Баффета](www.berkshirehathaway.com)

#### Как вывести текст с сохранением всех пробелов и переносов строк, которые мы написали в редакторе?

#### Можно ли использовать `<code>` без `<pre>`?


## Домашнее задание

Завершить [курс](https://code-basics.com/ru/languages/html)