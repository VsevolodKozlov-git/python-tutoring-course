# Урок 39. JavaScript 2

## Повторение ДЗ

#### В javascript есть методы и свойства, в Python есть методы и свойства. 

- Что между ними общего, а чем они отличаются?
- Чем отличается метод от свойства?
- Чем отличается функция от метода?

#### Какие типы данных в javascript являются неизменяемыми?


#### Что такое цепочка вызовов методов в JavaScript? 


#### Какие 2 способа задать функцию вы знаете? Чем они отличаются?

#### Может ли функция без return возвращать значения?

#### Что такое функция-предикат?

#### Отличаются ли как-то логические операторы `И`, `ИЛИ`, `НЕ` в python и JavaScript?

#### Пусть у вас есть 2 переменные `lastName` и `firstName`. Вам необходимо реализовать функцию: "Если есть `lastName`, то вернуть его, иначе вернуть `firstName`". Как вы её реализуете/


#### Чем конструкции условные конструкции отличаются в python и javascript?

#### Что такое тернарный оператор? Зачем он нужен?

#### Switch

Что такое swtich? Можно ли его заменить if else?

Что будет если убрать break в этом коде?
```js
const value = 1;
let result;
switch (value) {
    case 1:
        result = 'one';
        break;
    case 2:
        result = 'two';
        break;
    default:
        result = undefined
}

console.log(result);
```


#### Как завершить выполнение `while(true)`?

#### Чем отличается `i++` от `++i`?

## Материал урока

Создание первого проекта Vue.JS: [ссылка](https://ru.vuejs.org/guide/quick-start)

  - typescipt = no
  - Router = yes. Узнаем позже
  - prettier = yes. Будет приводить код к красивому виду
  - eslint = yes. Будет показывать ошибки в коде
  - Остальное нет


Запуск проект:

  - открываем проект в VS Code
  - npm install
  - npm run dev


Установить расширение Vue в VS Code


## Домашнее задание

- Создайте и запустите первый проект: [ссылка](https://ru.vuejs.org/guide/quick-start)
- Пройдите курс из 15 уроков по основам Vue.JS: [ссылка](https://ru.vuejs.org/tutorial/#step-1)
