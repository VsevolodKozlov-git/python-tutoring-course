# Урок 5. Функции, области видимости и циклы

## Вопросы

### Функции

#### Можно ли переприсвоить встроенные функции, такие как len и print или произойдет ошибка?

#### Как удалить переменную?

#### Как вызвать функцию?

#### Как называется то, что мы передаем в функцию? Зачем мы это передаем?

#### Что такое возвращаемое значение функции?

#### Что такое функция с побочными эффектами? Назовите 1 функцию с побочными эффектами и одну без них?

#### Как мы обозначаем аргументы функции, когда задаем ее? 

#### Переменные и аргументы - это одно и то же с точки зрения использования?

#### Чем отличается код в теле функции от кода, который мы пишем вне тела функции? 

Будет ли разница между
```python
def say_hello():
	print('hello world')

say_hello()
```
и 
```python
print('hello world')
```

#### Отличается ли чем-то вызов функции пользователя от вызова встроенной функции с точки зрения синтаксиса?

#### Зачем нужен return в функции?

#### Что возвращает `print('hello')`?

#### Что возвращает функция без return?

#### Что такое doc-строки? Зачем они нужны?

#### Можно ли функцию вложить в функцию? Зачем это может понадобиться?
### Циклы

#### Для чего существует цикл while? До каких пор он будет работать?

Что выведет код ниже
```python
n = 5
while n > 0:
	print(n)
	n -= 1
```

#### Для чего существует цикл for?

#### Как итерировать в цикле for по числам от 10 до 100?

#### Как итерировать в цикле for по всем знакам строки?

#### Можно ли цикл for всегда заменить циклом while?

#### Что такое вложенный цикл?

#### Для чего вам может понадобиться вложенный цикл?

### Области видимости

Что выведет этот код?
```python
x = 1

def print_x(x):
	print(x)

print_x(10)
```

Что выведет этот код?
```python
len = 'hello'

def print_length(x):
	print(len(x))

print_length('some long string')
```

Что выведет этот код?
```python
x = 1

def main_func(x):
	x = 10
	def innter_func():
		print(x + 1)


main_func(99)
```